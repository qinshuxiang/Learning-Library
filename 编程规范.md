## 编程规范

### 编程原则

#### 编码前思考

**不要假设。不要隐藏困惑。把权衡摆出来。**

动手实现之前：

- 明确声明你的假设。不确定就问。
- 存在多种理解时，全都摆出来，不要默默选定一种。
- 有更简单的做法就说出来，该反驳时反驳。
- 有不清楚的地方就停下来。指出困惑点。发问。



#### 简洁优先

**用最少的代码解决问题。不做任何推测性的设计。**

- 不加要求之外的功能。
- 不为一次性代码创建抽象。
- 不加没人要求的「灵活性」或「可配置性」。
- 不为不可能发生的场景写错误处理。
- 写了 200 行而其实 50 行就够，就重写。

问自己一句：「资深工程师会不会觉得这太复杂？」会，就简化。



#### 精准修改

**只碰必须碰的。只清理自己制造的混乱。**

编辑现有代码时：

- 不要「改进」相邻的代码、注释或格式。
- 不要重构没坏的东西。
- 沿用现有风格，哪怕你自己更习惯另一种写法。
- 发现无关的死代码，提一句即可，不要删。

当你的改动制造出孤儿代码时：

- 删除**因你的改动**而变得无用的导入、变量、函数。
- 不要删除本就存在的死代码，除非明确要求。

检验标准：每一行改动都应能直接追溯到用户的请求。



#### 目标驱动执行

**定义成功标准。循环验证直到达成。**

把任务转成可验证的目标：

- 「加校验」→「为非法输入写测试，然后让测试通过」
- 「修这个 bug」→「写一个能复现它的测试，然后让测试通过」
- 「重构 X」→「确保重构前后测试都通过」

多步任务要先给出简短计划：

```text
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]
```

强的成功标准让你能自行循环推进；弱的标准（「让它能跑」）会迫使你反复澄清。



### 执行结束

每个任务完成且验证通过后，将本次修改提交并推送到 GitHub main：

1. 先 git status，只添加本次任务相关文件。
2. 排除 .env、密钥、凭据、构建产物、缓存和无关改动。
3. 若已有测试/lint，必须通过；否则不要提交。
4. 若当前不在 main，先切到 main 并执行 git pull --rebase origin main。
   有冲突则停止并报告，不要自动解决。
5. 提交信息用：type(scope): 摘要，如 fix(auth): 修复登录过期问题。
6. 执行 git push origin main。
7. 禁止 force push、禁止跳过 hooks。
8. 若认证失败、分支保护、推送失败、无变更：停止并报告，等待人工处理。
9. 输出：提交 SHA、提交信息、推送结果。
