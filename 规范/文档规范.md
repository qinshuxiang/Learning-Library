## 文档规范

> [!IMPORTANT]
> **重要内容**：本规范规定文档的标题层级、空行留白、写作风格与禁用元素。编写或校改文档时按下列编号逐条判定，编号同时作为引用与自查的凭据。

### 判定顺序

判定顺序固定，逐项完成后进入下一项，不跳项、不并项：

1. 定层级：按 C1–C6 确定每个标题的级别、位置与文字。
2. 排留白：按 B1–B8 逐个标题、段落、引用块与警示框计算空行数。
3. 校风格：按 S1–S11 检查行文、术语、代码示例与提示载体。
4. 查禁用：按 X1 全文排查禁用元素。
5. 过清单：按「自查清单」逐项确认。

冲突处理：标题规则与空行规则重叠时以标题规则为准，见 B7。



### 标题层级

标题依靠井号的数量表达层级，正文各文件的层级约定如下：

- 二级标题，即文档标题：章节主标题，等于文件名去掉编号与点 md。
- 三级标题，即一级知识点：知识模块、大纲大块，如 基本概念。
- 四级标题，即二级知识点：具体考点、单一命题，如 概念的分歧。
- 五级标题，即三级知识点：当四级之下仍需细分时使用。

关于标题的硬性规则：

- C1：不使用一级标题。一篇文章等于一章，一级标题的语义层级留给整个学科，正文一律从二级标题起。
- C2：有且只有一个二级标题，且位于文件第一行。
- C3：层级不跳级，二级到三级到四级逐级下降。
- C4：未完成的模块保留空标题占位，标题下方留空行，保证大纲完整、后续可定位。
- C5：二级标题须与文件名严格一致。
- C6：标题不写 1.2、一、（一） 等序号，层级一律靠井号数量表达。



### 空行与留白

留白基准来自对范本正文全文标题的实测。空行数按下列规则判定：

- B1：二级标题之后空 1 行。
- B2：同一父标题下的首个同级标题，其前空 1 行；三级、四级、五级一律适用。
- B3：同级标题之间空 3 行；三级、四级、五级一律适用。
- B4：标题之后紧接正文时，标题与正文之间空 1 行。
- B5：段落之间空 1 行。
- B6：引用块与警示框前后各空 1 行。
- B7：两条规则重叠时以标题为准：紧邻同级标题之前的空行一律按标题规则计，首个同级标题前 1 行，其余同级标题前 3 行，不因 B6 改变行数。
- B8：文件末尾保留单个换行，不堆叠空行。

说明：技术类主题普遍不以速记收尾，每节末尾恰好停在引用块上，此处按 B7 执行。



### 写作风格

行文规则：

- S1：定义用判断句，以「某概念是什么」开头，一句话给全，不做铺垫。
- S2：术语首次出现即给定义，不假设读者已知。
- S3：不使用 我们、大家、其实、简单来说 等口语衔接，不写与读者的互动语。
- S4：技术类术语首次出现即给完整签名，写到方法就带参数。例如写出数组 splice 方法的 index、deleteCount、items 三个参数，而不只写方法名。
- S5：自定义函数使用驼峰命名法，名称言简意赅、英文直译，不加人名、缩写等特殊前缀；示例中的占位函数同样须用有意义的名字（如 getRandomInt、makeCounter），不用 fun1、fn、Foo 之类；构造函数用大驼峰（如 Person）。
- S6：方法、语法点尽量给出可运行代码块示例；纯概念性知识点（如面向过程与面向对象之别）不强制。

提示载体规则：

- S7：引用块作为普通提示与辨析的载体，常见用途为易混辨析、条文补充、例外情况、记忆口诀、理论同实务的分歧说明。
- S8：需强调「需要注意」「务必遵守」「后果严重」等更强语义时，改用警示框，不用普通引用块。判定：一般提示、辨析、补充用引用块；强调、强制、后果提示用警示框。
- S9：警示框是全库统一的提示强调方式，各领域文档均可使用；类型按语义择一，不得混用。五类对应 Typora 支持的 Github 风格标记：
  - 提醒用 NOTE，指读者应知、快览也应留意的一般信息。
  - 建议用 TIP，指可选的技巧或建议。
  - 重要用 IMPORTANT，指成功完成某事所必需的关键信息。
  - 警告用 WARNING，指因潜在风险要求立刻注意。
  - 注意用 CAUTION，指某操作可能带来的负面影响或需谨慎的后果。
- S10：警示框书写法为，在引用块首行书写 大于号、空格、半角方括号、感叹号、类型英文词、半角方括号，紧接一行后写正文。渲染端按标记词识别类型并着色加图标；本库以中文语境组织，故正文首句以对应的中文类型词加粗引领，便于未着色时仍可辨识语义。
- S11：警示框务必先类型后内容、一框一意。引用警示语时用对应的中文类型词加粗点题；不在一个框里罗列多个无关要点，也不嵌套警示框。



### 全库统一禁用的元素

下列元素不分领域一律不用：

- X1：正文中的裸 HTML 标签。
  - 判定：尖括号括起的元素名（p、div、table、link 等）写进正文句、标题、列表、引用块或警示框时，渲染端会当作真实 HTML 解析，导致文字被拆成段落、标签被吞、排版错乱。
  - 替代：表达元素一律写不带尖括号的普通文字，需要强调时加粗。
  - 例外：必须展示带尖括号的标签原文时，放入带 html 语言标注的围栏代码块。



### 自查清单

提交前逐项确认：

- 二级标题有且只有一个、位于文件第一行，且与文件名去掉编号与点 md 后严格一致。
- 全文无一级标题，标题未跳级，标题内无 1.2、一、（一） 等序号。
- 未完成的模块保留空标题占位，标题下方留空行。
- 各标题、段落、引用块与警示框的空行数符合 B1–B8；紧邻同级标题之前的空行取标题规则的值。
- 文件末尾为单个换行，未堆叠空行。
- 定义句为「某概念是什么」式判断句，一句话给全；术语首次出现即定义；无口语衔接与互动语。
- 技术术语首次出现带完整签名；自定义函数命名符合 S5；方法、语法点已给可运行示例，或属 S6 豁免的纯概念知识点。
- 提示载体选择正确：一般提示、辨析、补充用引用块，更强语义用警示框；警示框的类型、书写法与一框一意均符合 S9–S11。
- 全文无裸 HTML 标签；确需展示标签原文时已放入带 html 语言标注的围栏代码块。
