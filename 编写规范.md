# 学习类 Markdown 文档编写规范

> **本文档的效力范围**：仓库根 `/` 下的所有学习类 Markdown 文档。

---

## 一、目录结构规范

### 1.1 标准三层结构

```
仓库根/
├── <领域>/                      ← 一级：知识领域（2~4 字中文名词）
│   ├── <主题>/                  ← 二级：学科 / 课程 / 一门课
│   │   ├── 0. <主题>.md         ← 【必有】目录索引，编号固定为 0
│   │   ├── 1. <章节>.md         ← 正文，从 1 起编号
│   │   ├── 2. <章节>.md
│   │   └── assets/              ← 【可选】图片等二进制资源，与 md 同级
│   └── <主题>/
└── <领域>/<单篇>.md             ← 只有一个文件时，直接放领域层，不建目录
```

### 1.2 结构规则

| 编号 | 规则 | 说明 / 依据 |
|---|---|---|
| **S1** | 顶层按**知识领域**划分 | `法律`、`软件`、`常识`。目录名用 2~4 字中文名词，不加「类 / 篇 / 笔记 / 资料」等后缀 |
| **S2** | 二级为**主题**（学科/课程） | 一个主题一个目录，目录名即学科名 |
| **S3** | 每个主题目录**必有** `0. <主题>.md` 作索引 | `0.` 是索引专用槽位，正文永不占用 |
| **S4** | 正文从 `1.` 起连续编号 | 编号即学习顺序，也是考纲顺序 |
| **S5** | 文件名格式 `N. 名称.md` | 半角句点 + **一个空格** + 名称，如 `1. 法的本体.md` |
| **S6** | 编号不补零 | 现有 `10. 作用域与闭包.md`、`11. DOM 节点与 Web 存储.md` 均未补零，保持一致 |
| **S7** | 预计 **≥2 个章节**才建主题目录 + 索引；单篇直接放领域层 | 依据 `常识/月天数计算.md` |
| **S8** | 图片统一放 `<主题>/assets/` | 依据 `软件/前端/JavaScript/assets/` |
| **S9** | **不建空目录** | Git 不跟踪空目录；`公安/刑法/刑诉/民法/民诉/` 等空目录已从仓库移除 |
| **S10** | 文件名 = 文档内 H2 标题 = 索引条目显示名 | 三者一致，避免跳转错位 |

### 1.3 索引文件 `0. <主题>.md` 的格式

**唯一结构**（逐字符对照 `法律/法理学/0. 法理学.md`）：

```markdown
## 目录

 [1. 法的本体.md](1. 法的本体.md) 

 [2. 法的运行.md](2. 法的运行.md) 

 [3. 法的演进.md](3. 法的演进.md) 
```

拆解规则：

1. 首行是 `## 目录`（**H2，不用 H1**），后跟一个空行。
2. 每个条目占一行，格式为：
   `空格` + `[文件名](相对路径)` + `空格`
   —— 即 `·[N. 名称.md](N. 名称.md)·`（`·` 代表半角空格）。
3. 条目之间**空一行**。
4. 链接用**相对路径**、只写文件名（同目录），含空格无需转义，但必须做 URL 兼容书写。
5. 条目顺序与编号一致，新增章节时**追加到末尾**并同步编号。
6. 索引文件**只有** `## 目录` 及其条目，除此之外不写任何内容：不写参考来源说明、不写导言、不做分类、不加表格。

---

## 二、文档内容规范

### 2.1 标题层级

| 层级 | 语法 | 用途 | 实例（法理学） |
|---|---|---|---|
| 文档标题 | `##` | 章节主标题，等于文件名去掉 `N. ` 与 `.md` | `## 法的本体` |
| 一级知识点 | `###` | 知识模块 / 考纲大块 | `### 法的概念`、`### 法的价值` |
| 二级知识点 | `####` | 具体考点 / 单一命题 | `#### 法概念的争议`、`#### 国法` |
| 三级知识点 | `#####` | 仅当 `####` 下仍需细分时使用 | 技术类主题已启用，规则见 2.7 |

**硬性规则**：

- **C1｜不使用 H1（`#`）**。本仓库「一篇 md = 一章」，H1 的语义层级留给整个学科，故正文从 H2 起。
- **C2｜一个文件有且只有一个 H2**，且位于**文件第一行**。
- **C3｜层级不跳级**：`##` → `###` → `####` 逐级下降，不出现 `##` 直接接 `####`。
- **C4｜未完成的模块保留空标题占位**，标题下方留空行，保证大纲完整、后续可定位。
  实例：`### 法的价值`、`### 法的要素`、`### 法律关系` 目前均为空节。
- **C5｜H2 标题须与文件名严格一致**（见 S10）。
- **C6｜标题不写编号**：不写 `1.2`、`1.2.1`、`一、`、`（一）` 等序号，层级一律靠 `#` 的数量表达。
- **C7｜同级标题不重名**：同一父标题下的同级标题名称唯一，避免锚点冲突。
- **C8｜标题不使用行内代码（反引号）**：标题中出现的 API / 方法名直接裸写，不包反引号，如 `##### return() 方法` 而非 `##### `return()` 方法`。行内代码仅用于正文内容。

### 2.2 空行与留白

实测 `1. 法的本体.md` 的 21 处标题，得出下表：

| 位置 | 空行数 | 备注 |
|---|---|---|
| H2 标题之后 | **1** | |
| H3 标题之前 | **3** | 与上一节内容拉开，形成模块边界 |
| H3 标题之后 | **1** 或 **3** | 后接 H4 时为 1；后接正文段落时为 1 |
| H4 标题之前 | **3** | 同一 H3 下的**首个** H4 例外，为 **1** |
| H4 标题之后 | **1** | |
| H5 标题之前 | **3** | 同一 H4 下的**首个** H5 例外，为 **1** |
| H5 标题之后 | **1** | 规则同 H4 |
| 段落与段落之间 | **1** | |
| 引用块与前后段落之间 | **1** | |
| 代码块与前后之间 | **1** | 见 2.7 T3 |
| 文件末尾 | 保留单个换行 | 不额外堆空行 |

即：**同级标题之间隔 3 行空行，标题与紧邻内容隔 1 行空行**。这是本文档最容易走样也最影响阅读的地方，务必照做。

空行规则对**任意层级**通用：判断「首个」与否，只看上一个同级标题是否存在于当前父标题之下，与层级是 H3、H4 还是 H5 无关。

### 2.3 内容组织：考点的四段式结构

每个 `####` 考点按下列顺序展开，缺哪段就跳过哪段，但**顺序不可颠倒**：

1. **结论 / 定义先行** —— 首句直接给出该考点的判断标准或定义，一句话说清。
2. **要素拆分 / 分类展开** —— 用列表把要素、分类、情形拆开，一项一行。
3. **辨析 / 例外** —— 用引用块写易混点、例外、特殊情况、与相邻概念的差别。
4. **速记线索** —— 用引用块或加粗给出口诀、层级树、对照表。

**原文实例（`#### 法概念的争议`）**：

```markdown
#### 法概念的争议

法概念的争议核心在于法律是什么，即：法律和道德之间是否在**概念上**（非内容上）的**必然**（非偶然）联系。以此划分为两个阵营：法律实证主义和非实证主义，以内容的正确性为区分标准。

**法律实证主义**：**不承认**法律与道德存在概念上的必然的联系。

- **【实然法】恶法亦法**
- **非道德性要素**：**权威性制定**（官方认同）、**社会实效**（公众认同）
- **分类**：
  - **分析法学**：权威性制定为主，社会实效为辅。
  - **法社会学、法律现实主义**：社会实效为主，权威性制定为辅。

**非实证主义**：**承认**法律与道德存在概念上的必然的联系。

- **【应然法】恶法非法**
- **道德性要素**：**内容的正确性**（道德正确）
- **分类**：
  - **古典自然法学**：以内容的正确性为定义要素。
  - **第三条道路**：同时关注权威性制定、社会实效、内容的正确性。

**法律是什么在哲学上是没有答案的，理论立场的争论是没有对错的，"恶法非法"与"恶法亦法"没有对错之分，没有永远正确的立场，只有不断变化的需求**。



#### 马克思主义关于法的看法
```

对应四段式：**① 定义**（首段给出争议核心与划分标准）→ **② 分类展开**（两阵营各自的三要素 + 下属学派）→ **③ 辨析**（两阵营正反对比）→ **④ 速记**（结尾整句加粗的价值判断）。

### 2.4 强调与标记符号

| 符号 | 用途 | 实例 |
|---|---|---|
| `**加粗**` | 定义项、术语、判断关键词、对比双方 | `**法律实证主义**：**不承认**…` |
| `**术语**：` | 定义句式，术语加粗 + 全角冒号引领释义 | `**法律渊源**：是能够作为案件裁判的依据…` |
| `【标签】` | 考点标签：法律名、学说名、特征名 | `【实然法】`、`【应然法】`、`【罪刑法定】` |
| `【**加粗标签**】` | 并列的法定特征条目 | `1. 【**国家意志性**】法律是公共权力机构制定或认可的…` |
| `【短注】` | 句内纠错或限定，紧跟被限定词 | `【不是意志】`、`【填补漏洞】`、`【法律不清楚也要判】` |
| `> 引用块` | 见下方「引用块的五种用法」 | |
| 有序列表 `1.` | 有法定顺序或穷举性质的清单 | 宪法→法律→行政法规→…十类正式渊源 |
| 无序列表 `-` | 无顺序的要素、分类、特征 | 法的规范作用五类 |
| `**一、宪法**：` | 长清单的分项小标题，中文数字加粗 | 当代中国正式法律渊源十项 |
| `《》` | 法律、法规、司法解释、文件名 | `《立法法》第99条`、`《民法典》第10条` |
| `""` | 需特别区分的称谓或引述 | `"裁判依据"`、`"说理依据"` |

**引用块 `>` 的五种用法**（原文共 34 行引用块，全部落在以下五类）：

| 类型 | 作用 | 实例 |
|---|---|---|
| ① **易混辨析** | 与相邻概念划清边界 | `> 宪法学中的成文法与不成文法的区别在于是否成**法典**。` |
| ② **法条 / 官方规定补充** | 引出条文原文或司法解释 | `> 宪法是正式法律渊源，但是，在司法实践中，**《宪法》无法单独作为案件裁判的依据**…` |
| ③ **例外与特殊情况** | 主结论之外的但书 | `> 但是需要注意：**宪法、民族区域自治法、基本原则、专门规定不得变通**。` |
| ④ **记忆口诀 / 层级树** | 速记线索，可含嵌套列表 | `> 五级人大：国、省、市、县、乡` |
| ⑤ **理论 vs 实务的差异说明** | 标注通说与实务的分歧 | `> 在我国法理学通说中，司法解释**不属于正式法律渊源**…理论上的"非渊源"与实务中的"准法源"并存` |

**全局禁用的元素**（不分领域，一律不用）：

- Markdown 表格 —— 一律用列表表达层级；二维数据（字段表、端口表、位图）同样拆成「加粗定义项 + 冒号 + 释义」的列表项，不因「表格更好读」而破例。
- 水平分隔线 `---` / `***` —— 用标题层级划分区块，不用分隔线。
- 图标、彩色文本（过度装饰） —— ①②③ 等带圈字符不属于 Emoji，可使用。
- Emoji 可**适度**用于点缀（见 2.5 W10），但不可滥用，不得用表情替代语义。

> 行内代码 `` ` `` **不在禁用之列**。它曾出现在这份清单里，是因为清单依据的是 `1. 法的本体.md` 的实测计数（法理学确实 0 处），但那是**领域习惯而非全局事实**：技术类笔记实测有 759 处行内代码，标识符与 API 名必须包裹。技术类的完整要求见 2.7。

### 2.5 写作风格

- **W1｜定义用判断句**：以「X 是 Y」开头，一句话给全，不铺垫。
- **W2｜对比成对出现**：`**制定**是从无到有，**认可**是将现存的社会规范赋予法律效力` —— 加粗的两方必须同时出现。
- **W3｜限定词紧跟被限定词，并加粗**：`**概念上**（非内容上）的**必然**（非偶然）联系`。
- **W4｜结论性判断单独成段，整句加粗**：`**法律是有局限性的，局限性不可克服，只能尽力缓解，而缺点可以客服。**`
- **W5｜术语首次出现即给定义**，不假设读者已知。
- **W6｜不写**「我们」「大家」「其实」「简单来说」等口语衔接；不写与读者的互动语。
- **W7｜不臆造法条**：引用条文须给出名称与条号；不确定的表述写明「通说认为 / 实务中」，不绝对化。
- **W8｜中文标点**：全角逗号、句号、冒号、括号；并列项之间用顿号。
- **W9｜技术类术语首次出现即给完整签名**：写到方法就带参数，如 `arr.splice(index, deleteCount, ...items)`，不只写 `splice`。
- **W10｜技术类 Emoji 点缀**：标题、列表、引用块可少量使用 Emoji 区分知识模块，但同一文件不超过 3~5 处，且不重复堆砌，避免喧宾夺主。
- **W11｜函数命名**：自定义函数使用**驼峰命名法**，名称**言简意赅、英文直译**，不加人名、缩写等特殊前缀。示例代码中的占位函数同样须用有意义名（如 `getRandomInt`、`makeCounter`），不用 `fun1`、`fn`、`Foo` 等无意义名。构造函数仍用**大驼峰**（如 `Person`）。

### 2.6 占位文件规范

尚未开始写的分册，**只写一行 H2 标题**（与文件名一致），不加空行、不加正文、不加 `<!-- TODO -->`。

实例：`法律/法理学/5. 立法法.md` 全文即 `## 立法法`；`6. 习近平法制思想.md` 全文即 `## 习近平法治思想`。

已建好但完全为空的文件（`7. 宪法的基本理论.md`、`8. 国家基本制度.md`）应尽早补上 H2 标题，否则索引能跳转但页面无标题。

### 2.7 技术类主题的排版规则

适用 `软件/` 下的技术笔记（`前端/JavaScript`、`前端/CSS`、`计算机基础` 等）。这一节不是对前面规则的**豁免**，而是技术类**额外的**要求——技术类同样禁止表格、分隔线、Emoji，同样遵守 C1~C7。

- **T1｜代码块必须带语言标注**：写 ` ```js `，不写裸 ` ``` `。依据：现有 166 个围栏里只有 80 个带标注（48%），这是当前最实际的欠账。
- **T2｜标识符、API 名、文件名、命令一律用行内代码包裹**：`push()`、`Object.assign()`、`node_modules/`、`npm init -y`。
- **T3｜代码块前后各留 1 行空行**；代码里的补充说明写在代码注释中，不放在正文里另起一段复述。
- **T4｜H5（`#####`）仅在技术类使用**，空行规则同 H4（见 2.2）。依据：`3. JavaScript 内置对象.md` 有 35 处 H5，是全仓库唯一用例。
- **T5｜二维数据一律拆成列表**，不保留 Markdown 表格。转换形态示例：

```markdown
- **版本**（4 位）：IP 协议版本（IPv4 = 4）
- **首部长度**（4 位）：单位为 4 字节，最大 60 B（1111）
- **是否连接**：面向连接（TCP）／无连接（UDP）
- **H7~H1**：1 0 1 P3 0 P2 P1
```

即：3 列「名 / 值 / 说明」→ `- **名**（值 单位）：说明`；2 列对照 → `- **对比项**：A 方／B 方`；N 列位图 → 一行列出全部位值。
- **T6｜尽量配可运行示例**：每个 API / 方法 / 语法点尽量附一个带 ` ```js ` 标注的代码示例，而非纯文字介绍；纯概念性知识点（如「面向过程 vs 面向对象」）不强制。

---

## 三、模板

### 3.1 索引文件模板（`<主题>/0. <主题>.md`）

````markdown
## 目录

 [1. 第一章.md](1. 第一章.md) 

 [2. 第二章.md](2. 第二章.md) 

 [3. 第三章.md](3. 第三章.md) 
````

### 3.2 正文文件模板（`<主题>/N. <章节>.md`）

````markdown
## <章节名>

### <一级知识点一>

#### <考点一>

<结论 / 定义，一句话说清，判断句中的关键术语加粗。>

- **<要素一>**：<释义>
- **<要素二>**：<释义>
- **分类**：
  - **<类别一>**：<说明>
  - **<类别二>**：<说明>

**<对立 / 补充阵营>**：<与上文的对照表述>

- **【<标签>】<速记短语>**
- **<要素>**：<释义>

**<结论性判断，整句加粗，单独成段>。**



#### <考点二>

1. 【**<特征一>**】<完整表述>
   - <细分说明一>
   - <细分说明二>
2. 【**<特征二>**】<完整表述>

> <易混辨析：与相邻概念的边界 / 例外 / 法条补充 / 记忆口诀。>



### <一级知识点二>



### <一级知识点三（未写，保留占位）>
````

**填充要点**：区块之间隔 3 行空行；标题与正文隔 1 行；引用块前后各 1 行；文件末尾单个换行。

### 3.3 极简单页模板（单篇，不建目录）

适用：内容量小、不会拆章的杂项知识点。

````markdown
### <知识点名>

<正文，一段一意，段间空一行。>

> <补充 / 例外 / 口诀。>
````

依据：`常识/月天数计算.md`（`### 月天数计算` + 三句口诀 + 一条闰年规则引用块）。

---

## 四、自检清单

新建或改写一篇学习文档后，逐项核对：

**结构**

- [ ] 主题目录已有 `0. <主题>.md` 索引，且新章节已追加进去
- [ ] 索引文件只含 `## 目录` 与条目，未写参考来源说明、导言等（1.3 规则 6）
- [ ] 文件名符合 `N. 名称.md`（句点后一个空格、编号不补零）
- [ ] 文件名 = 文档 H2 标题 = 索引条目显示名
- [ ] 需要图片时已建 `assets/` 并放入，未散落在文档目录

**标题**

- [ ] 全文没有 `#`（H1）
- [ ] H2 只有一个，且在第一行
- [ ] 层级连续，无跳级
- [ ] 标题未写 `1.2.1`、`一、` 等序号（C6）
- [ ] 同级标题不重名（C7）
- [ ] 未完成的模块保留了空标题占位

**排版**

- [ ] H3 之前 3 行空行；H4 之前 3 行空行（同级首个为 1 行）
- [ ] H5 之前 3 行空行（同级首个为 1 行）
- [ ] 标题与紧邻内容之间 1 行空行
- [ ] 段落之间 1 行空行；引用块前后各 1 行空行
- [ ] 代码块前后各 1 行空行
- [ ] 文件末尾只有 1 个换行

**内容**

- [ ] 每个考点首句是定义或结论，不是背景铺垫
- [ ] 术语首次出现已给定义
- [ ] 对比项两方都加粗、成对出现
- [ ] 易混点 / 例外 / 法条补充都进了引用块，没有混在正文里
- [ ] 引用了法条且条号准确（或已标明「通说 / 实务」的分歧）
- [ ] 未使用表格、`---` 分隔线
- [ ] Emoji 使用克制、未滥用（W10）

**技术类（文档）**

- [ ] 代码块都带语言标注（T1）
- [ ] 标识符、API 名、文件名、命令都用行内代码包裹（T2）
- [ ] 代码里的补充说明写在注释中，未在正文另起一段复述（T3）
- [ ] 二维数据已拆成列表，未出现 Markdown 表格（T5）
- [ ] 标题不含行内代码 / 反引号（C8）
- [ ] 自定义函数用驼峰且有意义的英文名，占位函数不写 fun1 / fn / Foo（W11）
- [ ] 方法 / 语法点尽量配了代码示例（T6）
